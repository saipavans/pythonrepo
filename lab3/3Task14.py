def double_let(word):
	double_occurence_counter = 0
	while i < len(word)-1:
		if word[i] == word[i+1]
			double_occurence_counter+=1
			i+=2 # incrementing by 2, to avoid mistaking three 'aaa' as two time 'aa'
		else
			i+=1
			double_occurene_counter=0 ## break of pattern
		return (double_occurence_counter == 3)


					
